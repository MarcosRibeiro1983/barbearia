export interface ClientModel {
    id?: number;
    name: string;
    email: string;
    phone: string;
}

export interface SaveClientModel {
    name: string;
    email: string;
    phone: string;
}
